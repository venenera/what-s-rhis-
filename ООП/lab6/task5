#include <iostream>
#include <cmath>
using namespace std;

int main() {
    setlocale(LC_ALL, "rus");
    fraction M[100];
    fraction sum, t;
    fraction result(0, 1);
	char dummychar;
	int N = 0;
    do {
		cout << "Введите дробь: ";
		M[N++].putfraction();
		cout << "Продолжить ввод? (да - 'y', нет - 'n')? ";
		cin >> dummychar;
	} while (dummychar != 'n'); //цикл закончится при вводе ‘n’, продолжится при вводе ‘y’
    for (int i = 0; i < N; i++)
		result.frsum(arr[j]);
	result.frmult(fraction(1, N));
	cout << "Среднее значение: ";
	result.showfraction();
	cout << endl;
	return 0;
}
